<h1>Update {{team.name}}:</h1>
<form id="update-team" action="/people/team" method="post">
    Team Name: <input type="text" name="name" value="{{team.name}}"><br>
    Coach: <input type="text" name="coach" value="{{team.coach}}"><br>
    Location: <input type="text" name="location" value="{{team.location}}"><br>

    
    {{!--Promotion: <select name="organization" id="promotion-selector"> 
        {{#each promotion}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}--}}
    </select><br>

</form>
<button onclick="updateTeam({{team.id}})">Update</button>
<!--selectPromotion refer to below in the updatePerson.js -->
{{!--<script defer>selectPromotion({{person.organization}});</script>--}}